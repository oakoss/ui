# Changesets

This folder holds changesets — markdown files that describe how each PR should bump package versions. They're consumed by `pnpm changeset version` to update `package.json` versions and write CHANGELOG entries.

See [`docs/CONTRIBUTING.md`](../docs/CONTRIBUTING.md#changesets) for usage.

For the tool itself: <https://github.com/changesets/changesets>.
